{% extends "layout/base.html.twig" %}

{% block body_content %}
  <ul>
  {% for post in posts %}
    <li>
      <h2>{{ post.title }}</h2>
      {{ post.content }}
    </li>
	{% endfor %}
  </ul>
{% endblock %}
